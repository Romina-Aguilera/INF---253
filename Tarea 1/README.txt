Nombre:Romina Aguilera Otárola
Rol:202173572-0

Instrucciones de uso:
-Dentro de la carpeta donde estará este archivo debe existir un archivo con el nombre "codigo.txt" el cual contenga el código c-- a formatear
-Dentro de la carpeta donde estará este archivo debe existir un archivo con nombre "configuracion.txt" el cual contenga el formato de la configuracion 
deseada para el código a formatear
-Comando apara ejecutar: python3 formatter.py

Algunas consideraciones
- Si falla la estructura básica del código, "int main() { <codigo> return 0 ; }" el programa no sigue la ejecución porque ya lo detecta malo.
- Si encuentra un error en la estructura del if o while, no lo escribe por completo porque detecta la falla antes.